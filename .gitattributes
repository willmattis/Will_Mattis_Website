# Normalize text line endings; let git pick sane defaults.
* text=auto

# Treat these as binary (no line-ending conversion, no diffing).
*.step  binary
*.stp   binary
*.jpg   binary
*.jpeg  binary
*.png   binary
*.pdf   binary
*.xlsx  binary
*.mp4   binary
*.wasm  binary
*.svg   text
